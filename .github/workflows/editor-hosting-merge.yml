# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy Image Editor
"on":
  push:
    branches:
      - main
    paths:
      - "projects/image-editor/**"
  pull_request:
    branches:
      - main
    paths:
      - "projects/image-editor/**"

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install && npm run build-lib && npm run build-editor
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CODERMAN401_ANGULAR_DEMOS }}"
          channelId: live
          projectId: coderman401-angular-demos
          target: "image-editor-angular-coderman401"
